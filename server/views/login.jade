extends ./base

block page-content
	form(action="/login" + redirectUrl, method="post")
		label(for="user") Name
		input(type="text", name="user", id="user")
		label(for="password") Password
		input(type="password", name="password", id="password")
		input(type="submit")